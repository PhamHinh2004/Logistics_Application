spring:
  application:
    name: discovery-service
  config:
    import: "configserver:http://localhost:8088"
---
spring:
  config:
    activate:
      on-profile: docker
    import: "configserver:http://config-service:8088"