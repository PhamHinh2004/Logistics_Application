spring:
  data:
    mongodb:
      uri: mongodb://authentication_service:mongo123@mongodb:27017/authentication_service?authSource=admin
    redis:
      host: redis
      port: 6379
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: {"GOOGLE_CLIENT_ID"}
            client-secret: {"GOOGLE_CLIENT_SECRET"}
            scope: email, profile
          github:
            client-id: {"GITHUB_CLIENT_ID"}
            client-secret: {"GITHUB_CLIENT_SECRET"}
            scope: user:email
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8061/eureka


server:
  port: 9000

app:
  jwtSecret: aEgC6orb0GXD2sQwg2Vn3aTiVqmDMUtset7wi8mFJcY
  jwtExpirationMs: 86400000